import { Language } from './language.entity';
import { LanguageLevel } from './language-level.entity';

export interface LanguageLevelLocalize  {  id: number;   languageLevelId: number;  languageId: number;  name: string;   }